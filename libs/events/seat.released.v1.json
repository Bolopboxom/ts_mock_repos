{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "seat.released.v1",
  "type": "object",
  "properties": {
    "eventId": {"type": "string"},
    "correlationId": {"type": "string"},
    "timestamp": {"type": "string", "format": "date-time"},
    "version": {"type": "string"},
    "payload": {
      "type": "object",
      "properties": {
        "bookingId": {"type": "string"},
        "flightId": {"type": "string"}
      },
      "required": ["bookingId","flightId"]
    }
  },
  "required": ["eventId","correlationId","timestamp","version","payload"]
}
