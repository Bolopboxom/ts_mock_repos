{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "booking.cancelled.v1",
  "type": "object",
  "properties": {
    "eventId": {"type": "string"},
    "correlationId": {"type": "string"},
    "timestamp": {"type": "string", "format": "date-time"},
    "version": {"type": "string"},
    "payload": {
      "type": "object",
      "properties": {
        "bookingId": {"type": "string"},
        "reason": {"type": "string"}
      },
      "required": ["bookingId"]
    }
  },
  "required": ["eventId","correlationId","timestamp","version","payload"]
}
